# 変更履歴

[English](CHANGELOG.md) · **日本語**

バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。
修正は末尾の数字、機能追加は真ん中の数字、使い方が変わる変更は先頭の数字を上げます。
バージョンは `project.yml`（`MARKETING_VERSION`）の 1 か所だけで設定し、リリースごとに
`vX.Y.Z` のタグを付け、GitHub に `.ipa` ファイルを添付します。

## [1.7.0] — 2026-09-24

### ラボ
- 新しいラボ機能「**アプリドロワー**」を追加しました（初期値はオフ。オフのときはトレイや
  クリップボードの動作は何も変わりません）。オンにすると、トレイの下にショートカット一覧を
  全画面で表示するドロワーが使えます。登録できるのはインストール済みアプリ・ショートカット・
  URL スキーム・Web リンクの 4 種類で、インストール済みアプリの選択のみ TrollStore 限定、
  残り 3 種類は無料ビルドでも使えます。各項目にはカスタムアイコンを、ドロワー全体には
  背景画像を設定できます。
- ドロワーに何も登録していないときは、日付と現在の天気を表示します。天気は
  [Open-Meteo](https://open-meteo.com/) から取得し、位置情報を許可すればその場所の、
  許可しなければ手動で選んだ地域の天気になります。
- ドロワーは専用のタブになり、タブバーの先頭（アプリドロワー・トレイ・クリップボード・検索）に
  並びます（ラボのトグルがオンのときだけ表示）。ラボは設定内の独立したページになり、
  「取り込みの記録」のすぐ上に移りました。
- ドロワーのアイコンをタップすると実行し、長押しで編集します。カスタムアイコンを選ぶと、
  移動・拡大できる正方形の切り抜き画面が開きます。
- 修正：ドロワーをオンにすると正体不明のフォルダがトレイに入り、それを消すとドロワーに
  追加した内容がダイナミックアイランドに反映されなくなっていました。ドロワーのデータを
  ファイル App のフォルダに置かないようにしました。
- アイランド：トレイが空のときの余計な矢印をなくしました。項目があるときは、最初のページの
  左矢印でドロワーを開き、右矢印で戻ります。コンパクト表示の日付と曜日は中央揃えにしました。
- 修正：展開したアイランドでショートカットをタップしても IslandTray が開くだけでした。
  ライブアクティビティのリンクは必ず自分のアプリを開くため、アプリがショートカットの URL を
  引き継いで開くようにしました。
- ドロワーがオンのとき、トレイの最初のページに必ずドロワーへの左矢印が出るようにしました。
  これまではトレイのサムネイルで容量が埋まったときや、ショートカットが未登録のときに消えていました。
- ドロワーの矢印でアイランドがすぐ切り替わるようにしました（天気の更新を待たなくなりました）。
- アイランドのドロワーのアイコンを鮮明にしました。TrollStore 版（App Group あり）では、
  アイコンを高解像度のファイルとして保存し、アイランドが直接読み込むため 4 KB の制限を受けません。
  AltStore 版では、その制限内に収まるいちばん鮮明な JPEG で送ります。（Nightly 4・5 は HEIC で
  送っていたため、実機では灰色の仮表示になっていました。）
- 修正（Nightly 4）：アイランドとロック画面全体が灰色の仮表示になっていました。ウィジェットが
  タイルごとにアイコン画像を毎回デコードしていたのを、1 回の描画で 1 度だけにしました。
- ドロワー設定でアプリ名を非表示にすると、アイランドでも非表示になり、アイコンが大きくなります。
- ドロワー：アイコンを長押ししてドラッグすると並べ替えられます。↑↓ から名前順・追加が古い順・
  新しい順に並べ替えできます。
- アイランドのドロワーから「…」タイルをなくしました。空いている所をタップするとドロワーのタブが
  開きます（トレイ表示のときはトレイのタブ）。
- アイランド（またはロック画面）でトレイのサムネイルをタップすると、アプリでその項目の
  プレビューが開きます。
- 新しいトグル「ロック画面に常にドロワーを表示」：トレイの中身に関係なく、ロック画面の
  アクティビティにドロワーの先頭 6 個のアイコンを表示します。

### ツールバー
- 狭いウィンドウ（iPad の Slide Over）で収まらないツールバーのボタンは、
  名前つきでオーバーフローメニューに表示されます。

### アップデート
- 「重要なアップデートを毎回知らせる」をオンにしていると、起動時のアラートに加えて、
  重要なアップデートを初めて見つけたときに一度だけシステム通知でも知らせるようになりました。
  オフのときは今までどおり一度だけの案内で、通知も送りません。

## [1.6.2] — 2026-09-24

### iCloud 同期
- 別のデバイスの変更が、より速く反映されるようになりました。アプリを開いている間の
  フォルダの確認間隔を 30 秒から 15 秒に短縮し、さらに新しい項目を iCloud に要求した
  直後は、次の周期を丸ごと待たずに数秒後にもう一度確認します。新しい項目が 2 周期分
  待たされてから現れる、ということがなくなりました。

## [1.6.1] — 2026-09-24

### 設定
- セットアップの案内を分かりやすくしました。自動化がなぜ必要かの説明を、独立した
  見出しの下に置くのをやめ、それが説明する手順の先頭に移しました。その見出しも
  「設定手順」から、何のための設定かが分かる「ダイナミックアイランドを常に表示する」に
  変えました。
- 「状態」を「外観」のすぐ下に移しました。

## [1.6.0] — 2026-09-24

### iCloud 同期
- **iCloud 同期**を正式な機能にし、設定のいちばん上に移しました（1.6.0 Nightly 1 では
  ラボにありました）。初期値は引き続きオフです。オフのときはトレイとクリップボードは
  このデバイスだけに保存され、オンにして各デバイスで iCloud Drive の同じフォルダを選ぶと、
  両方の一覧がそこを通して共有されます。無料アカウントでも使えます。
- 別のデバイスの項目には雲のマークが付き、タップするとダウンロードされます。ボードの下の
  バーで同期の進み具合がわかり、タップすると詳細を確認できます。
- 項目を削除するとすべてのデバイスから削除されます。同期をオフにしたり、フォルダの接続を
  解除・変更したりしても何も削除されません。

### 設定
- 並び順を変更しました: iCloud 同期、アップデート、動作、外観、セットアップ、状態、取り込みの記録。
- ワンタップで入れるショートカットを、それぞれ対応する手順（共有シート、クリップボード）
  のセクションに置きました。
- アップデートに「リリースページを開く」を追加しました。最新かどうかにかかわらずいつでも、
  リリースノートや以前のビルドを確認できます。

## [1.6.0-nightly.1] — 2026-09-23 · Nightly

プレビュー版で、プレリリースとして公開します。アプリのアップデート確認は安定版だけを
見ているため、このビルドが案内されたり自動で切り替わったりすることはありません。

### ラボ
- 設定に「ラボ（実験的な機能）」を追加し、**iCloud 同期**を入れました（初期値はオフ）。
  オフのときはこれまでどおりトレイとクリップボードはこのデバイスだけに保存されます。
  オンにして各デバイスで iCloud Drive の同じフォルダを選ぶと、両方の一覧がそこを通して
  共有されます。無料アカウントでも使え、iCloud のエンタイトルメントは不要です。
- 別のデバイスの項目は名前の横に雲のマークが付き、タップするとダウンロードされます。
  同期やダウンロード中はボードの下にバーが出て、タップすると詳細（フォルダ、最終同期、
  アップロード待ち、履歴）を確認できます。
- 項目を削除するとすべてのデバイスから削除されます。同期をオフにしたり、フォルダの接続を
  解除・変更したりしても何も削除されません。

## [1.5.0] — 2026-09-23

### アップデート
- 設定に「重要なアップデートを毎回知らせる」を追加しました（初期値はオン）。オフにすると、重要なアップデートも通常のものと同じく一度だけ案内し、「このバージョンは今後表示しない」を選べます。

### クリップボード
- クリップボードのボードから項目をドラッグで取り出しても、書き出し時の設定にかかわらず項目は消えなくなりました。クリップボードなので、何度でも貼り付けられるよう残ります。項目を移動するのはトレイだけです。

## [1.4.0] — 2026-09-23

### ショートカット
- ShareToTray・ClipboardToTray で追加したとき、最後の「追加しました」ダイアログを出さず、ダイナミックアイランドが一瞬展開して緑のチェック（アラート音・触覚つき）で知らせるようにしました。ダイアログは追加に失敗したときと、アイランドを表示できないときだけ出ます。
- 2 つのショートカットの各アクションに固定の識別子を付けました。共有の確認で「常に許可」を選んでも項目ごとに聞かれていたのは、許可が「尋ねたアクションの識別子」に紐づくのに、アクションに識別子がなかったためです。
- iOS 27 では、クリップボード用ショートカットにスクリーンショットのオートメーションが入っています。一度オンにすれば、コピーしたスクリーンショットがそのままクリップボードのボードに入ります。設定に手順があります。（macOS 26 での署名はこのオートメーションを取り除きます。その場合はビルド時に警告が出ます。）

### 検索
- 内容でも検索できるようにしました。テキスト項目の中身と、画像内の文字（OCR）が対象です。検索画面の左上で「すべて／ファイル名／内容」を切り替えられます。

### アップデート
- 重要なアップデート（緊急の修正、または使い方が変わる更新）は、その旨を表示し、実際に更新するまで毎回知らせます。通常のアップデートはこれまでどおり一度だけ案内し、「表示しない」も選べます。

## [1.3.1] — 2026-09-22

### 修正
- フォルダ: ファイル App からのコピーを協調読み込みにし、フォルダの中身をファイル App に
  用意させるように。URL だけを渡してくるフォルダも取り込めます。それでも失敗したドロップは、
  設定の診断欄に理由が表示されます。

### 共有シート
- 共有シートのショートカットでフォルダも送れるように。

## [1.3.0] — 2026-09-22

### トレイ
- フォルダをそのままトレイに入れ、フォルダのまま取り出せるようになりました。ファイル App から
  ドラッグするか、「このiPhone内 → IslandTray」に保存してください。フォルダは 1 つの項目として、
  フォルダのアイコン・専用のグループとフィルタ・中身の合計サイズで表示され、ドラッグで取り出すと
  フォルダごと渡せます。

## [1.2.0] — 2026-09-21

### クリップボード
- 項目の名前をタップすると、iPhone のクリップボードにコピーされます。行を右にスワイプすると
  「コピー」、そのまま最後まで引くとすぐにコピー。グリッドでは長押しメニューに「コピー」が
  あります。テキストだけでなく画像やファイルもコピーできます。
- クリップボード用ショートカットの完了メッセージが「クリップボードに追加されました」になりました。

### 修正
- 共有シートのショートカットで追加すると、アプリを開かなくても Dynamic Island がすぐ更新されます。

## [1.1.5] — 2026-09-21

### 修正
- 1.1.4 は 2 つのショートカットが入らないままビルドされ、設定のインストール
  ボタンが動作していませんでした。元に戻しました。

## [1.1.4] — 2026-09-21

### 変更
- 「取り出したらトレイから削除する」と「元のファイルも削除する」を別々の設定に
  分けました（どちらも初期値はオフ）。元のファイルの削除に、トレイからの削除は
  不要になりました。

### 修正
- ドラッグで取り出すときはつねにコピーとして渡すようにしました。ファイル App が
  移動として扱わず、「+」が表示されます。

## [1.1.3] — 2026-09-21

### 変更
- インストールページを GitHub Pages
  （<https://pikare02.github.io/IslandTray/>）に移しました。アップデート通知からもこちらを開きます。

## [1.1.2] — 2026-09-21

### 修正
- iOS が知らない種類のファイル（`.ipa` など）を、ファイル App へドラッグで
  渡せるようにしました。一時的な種類として渡していたためファイル App に拒否され、
  「+」が表示されていませんでした。

## [1.1.1] — 2026-09-21

### アプリ
- 起動時のアップデート通知に「このバージョンは今後表示しない」を追加しました。
  それより新しいリリースは引き続き通知されます。

## [1.1.0] — 2026-09-21

### アプリ
- 起動時に GitHub で新しいリリースを確認し、インストールページを案内します。
  設定 →「アップデート」でオフにしたり、手動で確認したりできます。

### 変更
- 「取り出したらトレイから削除する」の初期値をオフ（切り取りではなくコピー）に
  変更しました。すでに設定済みの値はそのまま残ります。

## [1.0.0] — 2026-09-21

最初のリリースです。

### トレイ
- どのアプリからでもファイル・写真・動画・テキストをドラッグで入れられます。項目はアプリ内に
  コピーされるので、元が消えても残ります。
- ドラッグで取り出せます。別の指でほかのカードをタップすれば、まとめて運べます。
- グリッドとリストをツールバーで切り替え。名前・日付・サイズで並べ替え、種類ごとにグループ化、
  種類と期間で絞り込み、2 つのボードをまとめて検索できます。
- 複数選択して共有・削除。行を左にスワイプで削除、最後までスワイプすると即削除。
- 別のアプリに渡した項目をトレイから消す設定（コピーではなく切り取り）と、本当に移動した
  場合は元ファイルも削除する設定。
- トレイにすでにあるファイルを入れる前に確認します。

### Dynamic Island
- 件数・サムネイル・ファイル名を表示する Live Activity。ボタンで項目をめくれます。
- ショートカットのオートメーションで iOS の 8 時間制限を越えて表示し続けます。
  トレイが空でも表示する設定もあります。
- ショートカットで追加した内容を、別のアプリが前面にあってもすぐに反映します。

### 取り込み
- 共有拡張（有料アカウント）と、共有シート用の用意済みショートカット（無料アカウント）。
  どちらもアプリ内からインストールできます。
- *ファイルに保存 → このiPhone内 → IslandTray* に保存したものは、次にアプリを開いたときに
  取り込まれます。
- クリップボードのボード。アクションボタンや背面タップ用の用意済みショートカットで保存します。
  書式つきのテキストは、対応するアプリには書式ごと、それ以外にはテキストだけが貼り付けられます。

### アプリ
- 英語と日本語、ライト / ダークのテーマ、好きなハイライトカラー。
- TrollStore 用（`Release`）と AltStore / SideStore 用（`Free-Release`）の署名なし `.ipa`。

[1.3.1]: https://github.com/Pikare02/IslandTray/releases/tag/v1.3.1
[1.3.0]: https://github.com/Pikare02/IslandTray/releases/tag/v1.3.0
[1.2.0]: https://github.com/Pikare02/IslandTray/releases/tag/v1.2.0
[1.1.5]: https://github.com/Pikare02/IslandTray/releases/tag/v1.1.5
[1.1.4]: https://github.com/Pikare02/IslandTray/releases/tag/v1.1.4
[1.1.3]: https://github.com/Pikare02/IslandTray/releases/tag/v1.1.3
[1.1.2]: https://github.com/Pikare02/IslandTray/releases/tag/v1.1.2
[1.1.1]: https://github.com/Pikare02/IslandTray/releases/tag/v1.1.1
[1.1.0]: https://github.com/Pikare02/IslandTray/releases/tag/v1.1.0
[1.0.0]: https://github.com/Pikare02/IslandTray/releases/tag/v1.0.0
